% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dynamic.R
\name{nl_ode_solve}
\alias{nl_ode_solve}
\title{Solve a deterministic ODE model}
\usage{
nl_ode_solve(state, times, func, parms, method = "lsoda", ...)
}
\arguments{
\item{state}{Named initial state vector.}

\item{times}{Numeric output times.}

\item{func}{ODE derivative function accepted by \code{deSolve::ode}.}

\item{parms}{Named parameter vector/list.}

\item{method}{ODE solver method.}

\item{...}{Additional arguments to \code{deSolve::ode}.}
}
\value{
An \code{nlr_ode} object. Its \verb{$solution} element is the numeric matrix
returned by \code{deSolve::ode} (first column \code{time}, one column per state),
together with \verb{$times}, \verb{$states}, \verb{$parms}, \verb{$method}, \verb{$engine}, and
\verb{$diagnostics}. \code{print()} shows the solution, \code{summary()} lists the model
specification, and \code{[} and \code{as.data.frame()} are forwarded to the solution
matrix, so \code{sol[, "B"]} and \code{as.data.frame(sol)} keep working.
}
\description{
Routes initial-value ODE systems to \code{deSolve::ode}, enabling agronomic,
soil-process, physiological, and environmental dynamic models that do not
have a convenient closed-form nonlinear regression equation.
}
\examples{
\dontrun{
# Example 1: crop biomass with first-order approach to carrying capacity.
f <- function(t,state,parms) list(c(parms["r"]*(parms["K"]-state[1])))
nl_ode_solve(c(B=1),0:30,f,c(r=.08,K=20))
# Example 2: soil nutrient mineralization pool.
g <- function(t,state,parms) list(c(-parms["k"]*state[1]))
nl_ode_solve(c(N=100),0:60,g,c(k=.03))
# Example 3: physiological recovery after stress.
h <- function(t,state,parms) list(c(parms["k"]*(parms["Amax"]-state[1])))
nl_ode_solve(c(A=5),0:24,h,c(k=.2,Amax=30))
}
}
